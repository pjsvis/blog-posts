title: "pjsvis blog-posts"
tagline: "Actually, I was just thinking that..."
description: "Post-blog. Drop a node, close the file. The crows sort it out."

# GitHub Pages: project site → /blog-posts
baseurl: "/blog-posts"
url: "https://pjsvis.github.io"

# Show posts with future dates
future: true

# Engine
markdown: kramdown
kramdown:
  syntax_highlighter: rouge

# Collections
collections:
  posts:
    output: true
    permalink: /posts/:name/

  drafts:
    output: true
    permalink: /drafts/:name/

# Build exclusions
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/
  - scripts/
  - briefs/
  - debriefs/
  - decisions/

# Defaults
defaults:
  - scope:
      path: "_posts"
    values:
      layout: post
  - scope:
      path: "_drafts"
    values:
      layout: post
      published: false